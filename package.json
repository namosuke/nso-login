{
  "name": "nso-login",
  "version": "0.0.2",
  "description": "Login helper for Nintendo Switch Online",
  "author": "https://github.com/namosuke",
  "homepage": "https://github.com/namosuke/nso-login",
  "license": "MIT",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src/",
    "package.json"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.7.16",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3",
    "dotenv": "^16.0.2"
  },
  "scripts": {
    "dev": "ts-node -r dotenv/config src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js || echo NOTE: run `yarn build` first",
    "prepare": "rm -rf dist && yarn build"
  }
}
